// Modules are self contained -> used or removed without shit hahah
// Import codes/dependencies that we want to used -> export codes/dependencies
// Modules help us reduce the global namespace pollution

//Codes are just data -> way to control data

//tight coupling -> everything is connected

//AMD is developed for browsers -> created because people doesnt want to use native solutions such as common js -> requireJS helped us use AMD modules

//Named exports
//default export -> Can only have default export